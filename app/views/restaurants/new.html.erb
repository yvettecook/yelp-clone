<% if @restaurant.errors.any? %>
	<div id='errors' >
		<h2> <%= pluralize(@restaurant.errors.count, 'error') %> stopped this restaurant from being saved: </h2>
		<ul>
			<% @restaurant.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>


<%= form_for @restaurant do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.text_field :description %>
	<%= f.submit %>
<% end %>
